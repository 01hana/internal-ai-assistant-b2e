import { IdxSemanticError } from './semantic.error';
export function parseNativeClaims(credential: string): Readonly<Record<string, unknown>> { if (typeof credential !== 'string') fail(); const parts = credential.split('.'); if (parts.length !== 3 || parts.some((part) => !part) || !/^[A-Za-z0-9_-]+$/.test(parts[1]) || parts[1].length % 4 === 1) fail(); try { const bytes = Buffer.from(parts[1], 'base64url'); if (bytes.toString('base64url') !== parts[1]) fail(); const value: unknown = JSON.parse(new TextDecoder('utf-8', { fatal: true }).decode(bytes)); if (!plain(value)) fail(); return Object.freeze({ ...value }); } catch { fail(); } }
function plain(value: unknown): value is Record<string, unknown> { return typeof value === 'object' && value !== null && !Array.isArray(value) && Object.getPrototypeOf(value) === Object.prototype; } function fail(): never { throw new IdxSemanticError(); }
